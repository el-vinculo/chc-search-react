export const AND = 'AND';
export const OR = 'OR';
export const AND_NOT = 'AND NOT';

export const MODIFIER = {
  NOT: 'True',
  DEFAULT: 'False',
};

export const FILTER_TYPE = {
  TEXT: 'text',
  DROPDOWN: 'dropdown',
  SWITCH: 'switch',
};
